Fixed MaNGA cube loading. Uncertainty type is also handled properly now.

Fixed spectral axis value display in Markers plugin. Previously, it failed to display very small values, resulting in zeroes.
