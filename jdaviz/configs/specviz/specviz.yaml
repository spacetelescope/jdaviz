settings:
  configuration: specviz
  visible:
    menu_bar: false
    toolbar: true
    tray: true
    tab_headers: false
  context:
    notebook:
      max_height: 600px
toolbar:
  - g-data-tools
  - g-subset-tools
tray:
  - g-gaussian-smooth
  - g-collapse
  - g-model-fitting
  - g-unit-conversion
  - g-line-list
viewer_area:
  - container: col
    children:
      - container: row
        viewers:
          - name: Spectrum
            plot: specviz-profile-viewer
            reference: spectrum-viewer
